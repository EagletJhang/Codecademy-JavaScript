var cube = function (x) {
    return x * x *x;
};

cube(7);
console.log(cube("test")); 
//输出NaN not a number

//定义体积函数, 三个变量
var volume = function (w, l, h ) {
    if (typeof(x) !== 'number') return 0;
    //显示输入的数据类型
    return w * l * h;
};

volume(2, 3, 4);

